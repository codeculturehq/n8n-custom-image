apiVersion: v1
kind: Service
metadata:
  name: {{ include "n8n-custom.fullname" . }}
  labels:
    {{- include "n8n-custom.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "n8n-custom.selectorLabels" . | nindent 4 }}
